export const InitialState = {

}
export const FromReducer = (state, action) =>{
    switch(action.type){
        case "INPUT_VAL":
            return {

            }
        default:
            return state;
    }
}